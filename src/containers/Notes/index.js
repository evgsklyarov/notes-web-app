import React, { memo } from 'react';
import styled from 'styled-components';

const Notes = (props) => {
  return (
    <Container>
      {/*  ADD NOTES LIST AND CREATE ITEM COMPONENT */}
    </Container>
  );
};

const Container = styled.div`
  width: 100%;
  height: 100vh;
  display: flex;
  justify-content: space-between;
`;

export default memo(Notes);
